This project shows how interfaces can be created and "implemented" by
classes so that unrelated classes can be "grouped together" in
arrays, ArrayLists, or 2D arrays for processing by a single for loop
(polymorphism)
